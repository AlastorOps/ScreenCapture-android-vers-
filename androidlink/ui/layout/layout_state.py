"""Panel layout persistence placeholder.

Phase 9 will implement save/restore of splitter and panel geometry (show/hide,
rearrange, resize, restore defaults) via QMainWindow.saveState()/restoreState().
Phase 1's MainWindow uses a fixed splitter layout with no persistence.
"""
